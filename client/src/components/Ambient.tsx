/** Three soft colour glows that drift slowly behind everything. Decorative only. */
export default function Ambient() {
  return (
    <div className="ambient" aria-hidden>
      <i />
      <i />
      <i />
    </div>
  );
}
